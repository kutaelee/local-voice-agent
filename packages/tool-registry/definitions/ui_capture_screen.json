{
  "schema_version": "1.0",
  "name": "ui_capture_screen",
  "description": "Capture a window or full virtual desktop into the evidence store with pixel dimensions.",
  "risk_level": 0,
  "timeout_seconds": 30,
  "idempotency": "read_only",
  "parameters": {
    "type": "object",
    "properties": {
      "window_ref": {"type": "string", "minLength": 1, "maxLength": 512},
      "include_cursor": {"type": "boolean", "default": false}
    },
    "additionalProperties": false
  }
}
