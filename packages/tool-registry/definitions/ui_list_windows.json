{
  "schema_version": "1.0",
  "name": "ui_list_windows",
  "description": "List bounded visible top-level Windows UI windows and stable observation handles.",
  "risk_level": 0,
  "timeout_seconds": 15,
  "idempotency": "read_only",
  "parameters": {
    "type": "object",
    "properties": {
      "process_id": {"type": "integer", "minimum": 1},
      "title_contains": {"type": "string", "maxLength": 256},
      "limit": {"type": "integer", "minimum": 1, "maximum": 200, "default": 50}
    },
    "additionalProperties": false
  }
}
